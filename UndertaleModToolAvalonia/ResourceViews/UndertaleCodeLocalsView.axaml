<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:l="clr-namespace:UndertaleModToolAvalonia"
             xmlns:undertalem="clr-namespace:UndertaleModLib.Models;assembly=UndertaleModLib"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="UndertaleModToolAvalonia.UndertaleCodeLocalsView"
             x:DataType="l:UndertaleCodeLocalsViewModel" xmlns:strings="clr-namespace:UndertaleModToolAvalonia.Assets" >
    <Grid l:AutoGridBehavior.Enable="True" ColumnDefinitions="1*,3*" Classes="PaddedProperties">
        <Label Content="{x:Static strings:Strings.Name}"></Label>
        <l:UndertaleStringReferenceView Reference="{Binding CodeLocals.Name}" />

        <Label Content="{x:Static strings:Strings.Code_locals}"></Label>
        <l:EditableDataGrid ItemsSource="{Binding CodeLocals.Locals}" ItemFactory="{Binding CreateLocalVar}">
            <l:EditableDataGrid.Columns>
                <DataGridTemplateColumn Header="{x:Static strings:Strings.Index}" Width="1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="undertalem:UndertaleCodeLocals+LocalVar">
                            <TextBox Text="{Binding Index}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="{x:Static strings:Strings.Name}" Width="3*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="undertalem:UndertaleCodeLocals+LocalVar">
                            <l:UndertaleStringReferenceView Reference="{Binding Name}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </l:EditableDataGrid.Columns>
        </l:EditableDataGrid>
    </Grid>
</UserControl>
